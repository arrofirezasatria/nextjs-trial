import React from "react";
import ProductCardButton from "./ProductCardButton";

const ProductCard = () => {
  return (
    <div>
      <ProductCardButton />
    </div>
  );
};

export default ProductCard;
